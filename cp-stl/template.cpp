#include <bits/stdc++.h>
using namespace std;

using i64 = long long;
using u64 = unsigned long long;
using i128 = __int128_t;

#ifdef LOCAL
#include "util/debug.hpp"
#else
#define debug(...) ((void)0)
#endif

void solve() {
    // 在这里写单组数据的解法。
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;  // 题目有多组数据时再打开。
    while (t--) solve();
    return 0;
}
